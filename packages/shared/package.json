{
  "name": "@gs/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types + pure helpers for game-servers Workers (registry schema, RPC contracts, registry build). No AWS/OIDC logic (ADR 0004).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./registry-types": "./src/registry-types.ts",
    "./rpc-types": "./src/rpc-types.ts",
    "./auth-types": "./src/auth-types.ts",
    "./build": "./src/build.ts",
    "./registry-index": "./src/registry-index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
