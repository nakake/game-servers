{
  "name": "@game-servers/sidecar",
  "version": "0.0.1",
  "private": true,
  "description": "Sidecar container — idle detection + heartbeat for game-servers (Phase 3). Runs alongside the game container on each EC2 instance, polls the game via adapter (e.g. minecraft RCON), and reports to the Cloudflare Worker over HMAC-signed HTTPS.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.700.0",
    "rcon-client": "^4.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
